# Learn more about configuring your app at https://shopify.dev/docs/apps/tools/cli/configuration

client_id = "bbd179e9f5f501e3917e14dd5297389c"
name = "seo-app"
application_url = "https://example.com"
embedded = true

[build]
automatically_update_urls_on_dev = true
include_config_on_deploy = true

[webhooks]
api_version = "2026-04"

  # Compliance / GDPR — obligatorios para aprobar el review del App Store.
  [[webhooks.subscriptions]]
  compliance_topics = [ "customers/data_request" ]
  uri = "/webhooks/customers/data_request"

  [[webhooks.subscriptions]]
  compliance_topics = [ "customers/redact" ]
  uri = "/webhooks/customers/redact"

  [[webhooks.subscriptions]]
  compliance_topics = [ "shop/redact" ]
  uri = "/webhooks/shop/redact"

[access_scopes]
# Learn more at https://shopify.dev/docs/apps/tools/cli/configuration#access_scopes
# read_products  → leer productos, imágenes y campos SEO (core del análisis)
# read_content   → leer páginas y colecciones (análisis extendido en plan Pro)
# write_products → bulk fix de alt texts (solo plan Pro)
scopes = "read_products,read_content,write_products"

[auth]
redirect_urls = [ "https://example.com/api/auth" ]
